#include <stdio.h>

int count_a(seq[char] list) {
    int count = 0;
    while (*list != '\0') {
        count += *list == 'a';
        list++;
    }
    return count;
}

int main() {
    arr[char] myString = "hello, warld";
    int count = count_a(myString);
    printf("%d\n", count);
}
